# RFC Process

Use an RFC when a change is:

- user-visible and cross-cutting
- hard to reverse
- a compatibility model shift
- a new public config or CLI contract
- a governance or policy change

Do not use an RFC for:

- typo fixes
- minor clarifications
- invisible implementation details
