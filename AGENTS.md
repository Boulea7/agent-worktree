# agent-worktree Shared Instructions

This file is the repository-specific execution rules and boundary companion for coding agents. For canonical repository truth, start with `SPEC.md`, then `README.md`, then `docs/index.md`.

## Project Purpose

`agent-worktree` is building a git-native orchestration layer for coding agents that:

- uses isolated worktrees as the scheduling primitive
- treats deterministic validation as the selection primitive
- stays compatible with multiple agent tools instead of locking to one vendor

## Current Phase

This repository is in an early implementation phase.
The current public baseline is the thin Phase 4 compatibility baseline, built on the existing Phase 2 worktree lifecycle and bounded Phase 3 `codex-cli` execution foundations, while current internal continuation work now spans the thin Phase 5 closeout chain plus bounded-parallelism Phase 6 prep.
This repository now has a usable Git `HEAD` baseline, so maintainer workflows should prefer non-destructive worktree isolation and commit-backed checkpoints for each completed thin slice.

Agents working in this repository should prioritize:

1. contract fidelity for config, manifests, and machine-readable CLI behavior
2. test coverage and deterministic validation
3. thin worktree lifecycle slices before richer runtime orchestration
4. explicit public/private doc boundaries

The currently implemented Phase 2 slice includes:

- `attempt create`
- `attempt list`
- safety-first `attempt cleanup`

The current thin Phase 4 public compatibility slice now includes:

- read-only `doctor` diagnostics for adapter truth and local detection state
- read-only `compat probe <tool>` diagnostics for bounded per-runtime compatibility results
- read-only `compat smoke <tool>` diagnostics for bounded env-gated live compatibility results
- explicit allow-list CLI JSON serializers for `doctor`, `compat list`, `compat show`, `compat probe`, `compat smoke`, `attempt create`, `attempt list`, and `attempt cleanup`, so additive internal DTO or manifest fields do not silently widen the public machine-readable contract
- an explicit public cleanup-outcome vocabulary for `attempt cleanup --json`, so additive internal cleanup outcomes do not silently widen the public machine-readable contract

That public compatibility baseline now satisfies the current Phase 4 exit criteria: `codex-cli` has a bounded public compatibility smoke path through `compat smoke`, while the other Tier 1 runtimes retain explicit descriptor-only support boundaries through `doctor` and `compat probe`. That remains a compatibility promise only; it does not imply general public execution or lifecycle support.

The current thin Phase 5 internal verification and selection slice now spans these capability buckets:

- verification ingestion, execution, artifact-summary, and deterministic comparison helpers
- selection, promotion, handoff, and handoff-finalization composition helpers
- internal control-plane and runtime-state composition derived from bounded execution observation and attempt lineage
- delegated/headless bridge work plus wait- and close-oriented preflight or consume paths
- intentionally narrow default barrels, with wider repo-internal staging surfaces kept separate

Recent thin-slice follow-ups inside those buckets now also include:

- canonical wait/close consume request-contract guardrails, so malformed internal requests fail before injected wait/close invokers see them
- top-level wait/close consumer guardrails, so malformed `consumer` and `consumer.readiness` containers now fail with bounded validation errors instead of leaking raw runtime `TypeError`s
- canonical spawn request-contract guardrails and apply-side pre-invoker derivation, so malformed internal spawn requests or child-lineage inputs now fail before injected spawn invokers see them
- direct spawn projection guardrails for `spawn_recorded_event` and `spawn_headless_input`, so malformed requested-event or execution-seed inputs now fail with bounded validation errors instead of leaking raw runtime `TypeError`s
- stricter `spawn_headless_input` lineage, abort-signal, and guardrail normalization, so delegated child payloads keep required parent/source metadata, reject AbortSignal-shaped impostors, and collapse empty guardrails without widening internal contracts
- earlier promotion-side `sourceKind` validation, so invalid attempt provenance now fails in the current promotion candidate/result seams instead of drifting into later explanation or audit layers
- internal close-side `apply`, `apply-batch`, and `target-apply` helpers, so the current close-oriented control-plane chain now reaches the same bounded composition layer as the existing wait-side path without widening public lifecycle or execution surfaces
- internal wait/close `target-apply-batch` helpers, so both target-oriented convenience paths now reach the same bounded internal batch layer without widening public lifecycle or execution surfaces
- a single repo-internal handoff-finalization closeout compositor that chains request-summary apply through closure-summary derivation without widening public surfaces
- a narrow closeout-decision gate above that closeout composer, so later repo-internal consumers can derive blockers from closure results without widening public review, merge, or lifecycle policy
- a single repo-internal closeout-decision entry helper above that chain, so later repo-internal callers can derive the current closeout decision through one bounded composition step without inventing a broader consumer contract
- stricter public compatibility and attempt serializer string-slot validation plus cloned compat-catalog reads, so additive DTO drift or repo-internal descriptor mutation does not silently widen machine-readable public output
- a repo-internal spawn-budget projection layer above the current spawn-readiness seam, so existing guardrails now also derive explicit remaining child-slot and depth-allowance state for later internal bounded-parallelism work without widening readiness vocabulary or public surfaces
- a repo-internal spawn-candidate follow-up that now composes existing budget projection with context and readiness into one current candidate object for later internal consumers, without widening spawn-target, spawn-request, or public orchestration contracts
- a repo-internal spawn-batch-plan follow-up above that candidate seam, so later internal consumers may evaluate proposed sibling batch counts against current remaining child-slot budget without widening spawn-target, spawn-request, spawn-apply, or public orchestration contracts
- a repo-internal spawn-batch-items follow-up above that planning seam, so later internal consumers may project explicit child attempt identifiers plus a source kind into ordered spawn batch items without widening spawn-apply, headless execution, queue truth, or public orchestration contracts
- a repo-internal spawn-batch-items-apply convenience seam above that planning bridge, so later internal consumers may compose existing batch items directly into the current spawn-apply-batch path without widening headless execution, queue truth, scheduler truth, or public orchestration contracts
- a repo-internal spawn-batch-headless-apply-items projection seam above that planning bridge, so later internal consumers may pair existing batch items with ordered headless execution seeds before the current headless apply/execute batch path without widening headless execution truth, queue truth, scheduler truth, or public orchestration contracts
- a repo-internal spawn-batch-headless-apply convenience seam above that projection seam, so later internal consumers may compose existing headless-apply items directly into the current spawn-headless-apply-batch path without widening headless execution truth, queue truth, scheduler truth, or public orchestration contracts
- internal spawn-headless wait/close `target-apply` and `target-apply-batch` helpers, so the current headless batch bridge now reaches the same bounded convenience layer as the generic wait/close target-oriented apply path without widening public lifecycle or execution surfaces
- a repo-internal spawn-headless wait `request` and `request-batch` seam above the current headless wait-target wrappers, so later internal consumers may reuse canonical generic wait requests without invoking wait-consumer/apply paths or widening public lifecycle or execution surfaces
- a repo-internal spawn-headless close `request` and `request-batch` seam above the current headless close-target wrappers, so later internal consumers may reuse canonical generic close requests without invoking close-consumer/apply paths or widening public lifecycle or execution surfaces
- shared-fixture traceability assertions across the current spawn-batch-headless-apply seam and downstream wait/close target batches plus target-apply batches, so maintainers now have one evidence-backed pairing between upstream seam ordering/lineage checks and downstream blocked or applied target derivation without widening public lifecycle or execution surfaces
- stricter spawn-headless wait/close `target-apply` wrapper guardrails plus direct apply-to-generic traceability assertions, so malformed headless wrappers now fail with bounded validation errors instead of silently collapsing into blocked wrappers, while the current headless target-apply results stay explicitly aligned with the generic wait/close target-apply semantics without widening public lifecycle or execution surfaces
- stricter `spawn-batch-headless-apply-items` execution projection plus stronger shared-fixture hardening, so the current batch bridge now keeps execution seeds on the canonical `prompt` / `cwd` / `timeoutMs` / `abortSignal` vocabulary, avoids silent extra-field drift before downstream headless-input normalization, and keeps batch test fixtures aligned with the real projected `batchItems.items` shape without widening internal contracts
- repo-internal close-side and closeout-entry barrel assertions, so current tests explicitly prove those helpers stay available only through internal barrels and do not leak into default entry points
- bucket-level repo-internal barrel coverage for `selection`, `control-plane`, and `verification`, plus repo-internal type-only denylist coverage and narrower default `control-plane` / `selection` / `verification` entry points, so tests keep proving boundaries without freezing helper-by-helper topology as a docs contract

That Phase 5 foundation remains internal-only. It does not introduce public verification, ranking, promotion, handoff, report, explanation, decision, target, consumer, queue, persistence, or broader lifecycle surfaces.
Required verification checks that end in `skipped` remain blocking for Phase 5 selection and promotion semantics; they must not be treated as satisfied gates.
When repo-internal code needs wider helper access, prefer dedicated internal barrels or concrete module paths instead of widening the default entry points into a de facto public helper surface.

The current thin Phase 3 foundation also includes:

- static adapter descriptors derived from the shared compatibility vocabulary
- capability-first adapter resolution
- render-only command contracts for the initial `codex-cli` reference adapter
- structured degradation for unsupported adapter capabilities
- a bounded internal `codex-cli` execution contract for detect, `codex exec --json`, minimal canonical event parsing, and an env-gated smoke scaffold
- a bounded internal `codex-cli` profile-aware execution path that may pass an explicit `--profile` through project-managed `codex exec --json` calls without becoming provider management, a public CLI surface, or manifest-backed state
- a bounded internal `codex-cli` relay-compatible execution-env helper that may derive subprocess env overlays from local Codex config, auth, or shell-export state without becoming a public provider-management contract
- a bounded internal `codex-cli` default-runner probing path that may reuse that relay-compatible env overlay on detect, probe, smoke preflight, and executable resolution as a best-effort default-runner behavior only, while custom runners still keep their original environment unless an internal caller explicitly supplies a replacement environment resolver
- a minimal internal session-tree/control-plane foundation derived from attempt provenance and bounded execution observation, implemented as pure helpers only
- a bounded internal execution-to-control-plane bridge for `codex-cli`, where supplied attempt lineage may derive an internal session snapshot in the execution result
- a minimal internal runtime-state foundation that may derive execution-session records and indexes from lineage, observation, and optional internal session snapshots without persisting them
- a minimal internal runtime-state read model that may build queryable execution-session views from derived records without becoming a lifecycle manager or registry
- a minimal internal runtime-context layer that may derive a single-consumer execution-session context from the runtime-state read model without becoming lifecycle control, persistence, or runtime truth
- a minimal internal lifecycle-disposition layer that may derive shared terminal/session-known/descendant-impact facts from runtime-context without becoming lifecycle truth
- a minimal internal spawn-readiness layer that may derive guardrail-aware delegated-child preflight blockers from runtime-context plus runtime-state views without becoming actual spawn support, public selectors, or lifecycle truth
- a minimal internal spawn-candidate layer that may compose an existing execution-session context plus spawn-budget and spawn-readiness into a single-consumer internal object without becoming actual spawn support, public selectors, or lifecycle truth
- a minimal internal spawn-batch-plan layer that may compose an existing spawn-candidate plus a proposed sibling batch count into a bounded internal planning projection for later bounded-parallelism work, but it must not become request truth, apply truth, scheduler truth, queue state, runtime enforcement, manifest-backed budget truth, or a public orchestration contract
- a minimal internal spawn-batch-items layer that may compose an existing spawn-batch-plan plus explicit child attempt identifiers and a source kind into ordered spawn batch items for later internal batch consumers, but it must not become apply truth, headless execution truth, scheduler truth, queue state, runtime enforcement, manifest-backed batch truth, or a public orchestration contract
- a minimal internal spawn-batch-items-apply helper that may compose an existing spawn-batch-items result directly into the current spawn-apply-batch path as a bounded convenience seam, but it must not become headless execution truth, queue truth, scheduler truth, manifest-backed batch truth, or a public orchestration contract
- a minimal internal spawn-batch-headless-apply-items helper that may compose an existing spawn-batch-items result plus ordered headless execution seeds into request-preserving headless-apply batch items for later internal batch consumers, but it must not become headless execution truth, queue truth, scheduler truth, manifest-backed batch truth, or a public orchestration contract
- a minimal internal spawn-batch-headless-apply helper that may compose an existing spawn-batch-headless-apply-items result directly into the current spawn-headless-apply-batch path as a bounded convenience seam, but it must not become headless execution truth, queue truth, scheduler truth, manifest-backed batch truth, or a public orchestration contract
- a minimal internal spawn-target layer that may project a minimal internal spawn-oriented target from an existing spawn-candidate without becoming actual spawn support, public selectors, or lifecycle truth
- a minimal internal spawn-request layer that may compose an existing spawn-capable candidate plus an explicit child source choice and optional inherited guardrails into a future internal spawn-oriented request object without becoming actual spawn support, public selectors, or lifecycle truth
- a minimal internal spawn-lineage layer that may derive additive child attempt lineage from a spawn-request plus an explicit child attempt identifier without becoming actual spawn support, manifest-backed lifecycle truth, public selectors, or child-planning semantics
- a minimal internal spawn-requested-event layer that may derive a minimal parent-session `spawn_requested` marker from an existing spawn-request without becoming actual spawn support, child-creation truth, public selectors, or lifecycle truth
- a minimal internal spawn-recorded-event layer that may derive a second minimal parent-session `spawn_recorded` marker from an existing spawn-requested-event without becoming actual spawn support, child-creation truth, public selectors, or lifecycle truth
- a minimal internal request-based spawn-consume helper driven by an explicitly injected invoker that may consume an existing spawn-request without becoming child-creation truth, public selectors, or lifecycle truth
- a minimal internal request-based spawn-consume-batch helper that may consume an explicit ordered list of existing spawn-requests through that same injected invoker, preserve input order, and fail fast on the first injected invoker error without becoming child-creation truth, public selectors, or lifecycle truth
- a minimal internal spawn-effects helper that may compose minimal child-lineage plus parent-session `spawn_requested` and `spawn_recorded` markers from an existing spawn-request and explicit child attempt identifier without becoming child-creation truth, terminal lifecycle truth, public selectors, or actual spawn support
- a minimal internal spawn-effects-batch helper that may compose an explicit ordered list of those same spawn-effects results, preserve input order, and fail fast on the first derivation error without becoming child-creation truth, terminal lifecycle truth, public selectors, or actual spawn support
- a minimal internal request-based spawn-apply helper driven by an explicitly injected invoker that may compose existing spawn-consume metadata plus spawn-effects metadata without becoming child-creation truth, terminal lifecycle truth, public selectors, or actual spawn support
- a minimal internal request-based spawn-apply-batch helper that may compose an explicit ordered list of those same spawn-apply results through that same injected invoker, preserve input order, and fail fast on the first error without becoming child-creation truth, terminal lifecycle truth, public selectors, or actual spawn support
- a minimal internal spawn-headless-input helper that may compose existing spawn-effects metadata plus a minimal headless-execution seed into a future internal execution input with `attempt` derived from child lineage without becoming child-creation truth, terminal lifecycle truth, public selectors, or actual spawn support
- a minimal internal spawn-headless-input-batch helper that may compose an explicit ordered list of those same spawn-headless-input results, preserve input order, and fail fast on the first bridge error without becoming child-creation truth, terminal lifecycle truth, public selectors, or actual spawn support
- a minimal internal request-based spawn-headless-apply helper driven by an explicitly injected spawn invoker that may compose existing spawn-apply metadata plus a whitelisted headless-execution seed into a bounded internal headless payload without becoming child-creation truth, terminal lifecycle truth, public selectors, or actual delegated runtime support
- a minimal internal request-based spawn-headless-apply-batch helper that may compose an explicit ordered list of those same spawn-headless-apply results through that same injected invoker, preserve input order, and fail fast on the first error without becoming child-creation truth, terminal lifecycle truth, public selectors, or actual delegated runtime support
- a minimal internal spawn-headless-execute helper that may compose an existing spawn-headless-apply result plus an explicitly injected bounded headless executor into internal execution-consumption metadata without becoming child-creation truth, terminal lifecycle truth, public selectors, or actual delegated runtime support
- a minimal internal spawn-headless-execute-batch helper that may compose an explicit ordered list of those same spawn-headless-execute results through that same injected executor, preserve input order, and fail fast on the first error without becoming child-creation truth, terminal lifecycle truth, public selectors, or actual delegated runtime support
- a minimal internal spawn-headless-record helper that may compose an existing spawn-headless-execute result plus generic execution-session record derivation into derived runtime-state metadata without becoming child-creation truth, terminal lifecycle truth, public selectors, or actual delegated runtime support
- a minimal internal spawn-headless-record-batch helper that may compose an explicit ordered list of those same spawn-headless-record results, preserve input order, and fail fast on the first derivation error without becoming child-creation truth, terminal lifecycle truth, public selectors, or actual delegated runtime support
- a minimal internal spawn-headless-view helper that may compose an existing spawn-headless-record result plus generic execution-session view derivation into derived runtime-state view metadata without becoming child-creation truth, terminal lifecycle truth, runtime truth, public selectors, or actual delegated runtime support
- a minimal internal spawn-headless-view-batch helper that may compose an existing spawn-headless-record-batch result plus shared execution-session view derivation into derived batch runtime-state view metadata without becoming child-creation truth, terminal lifecycle truth, runtime truth, public selectors, or actual delegated runtime support
- a minimal internal spawn-headless-context helper that may compose an existing spawn-headless-view result plus generic execution-session context derivation into derived runtime-context metadata without becoming child-creation truth, terminal lifecycle truth, runtime truth, public selectors, or actual delegated runtime support
- a minimal internal spawn-headless-context-batch helper that may compose an explicit ordered list of those same internal context derivations from an existing spawn-headless-view-batch result while reusing the same shared execution-session view, preserving selector order, and failing fast on the first error without per-item summary contracts or becoming child-creation truth, terminal lifecycle truth, runtime truth, public selectors, or actual delegated runtime support
- a minimal internal spawn-headless-wait-candidate helper that may compose an existing spawn-headless-context result plus generic wait-readiness derivation into bounded internal wait-candidate metadata without calling the generic selector-driven wait-candidate helper or becoming child-creation truth, runtime truth, terminal lifecycle truth, polling truth, timeout scheduling truth, public selectors, or actual wait support
- a minimal internal spawn-headless-wait-candidate-batch helper that may compose an explicit ordered list of those same bounded internal wait-candidate derivations from an existing spawn-headless-context-batch result, preserve input order, and fail fast on the first readiness error without per-item summary contracts or becoming child-creation truth, runtime truth, terminal lifecycle truth, polling truth, timeout scheduling truth, public selectors, or actual wait support
- a minimal internal spawn-headless-wait-target helper that may compose an existing spawn-headless-wait-candidate result plus generic wait-target derivation into bounded internal wait-target metadata without becoming actual wait support, child-creation truth, runtime truth, terminal lifecycle truth, public selectors, or delegated-runtime truth
- a minimal internal spawn-headless-wait-target-batch helper that may compose an explicit ordered list of those same bounded internal wait-target results from an existing spawn-headless-wait-candidate-batch result while reusing the same single-item helper, preserving input order, keeping blocked entries in place, and failing fast on the first wait-target derivation error without partial-result summaries or becoming actual wait support, child-creation truth, runtime truth, terminal lifecycle truth, public selectors, or delegated-runtime truth
- a minimal internal spawn-headless-close-candidate helper that may compose an existing spawn-headless-context result plus capability-aware close-readiness into a bounded internal close-oriented bridge without becoming actual close support, child-creation truth, runtime truth, terminal lifecycle truth, public selectors, or delegated-runtime truth
- a minimal internal spawn-headless-close-candidate-batch helper that may compose an explicit ordered list of those same internal close-oriented bridges from an existing spawn-headless-context-batch result while reusing the same single-item helper, preserving input order, and failing fast on the first readiness-derivation error without partial-result summaries or becoming actual close support, child-creation truth, runtime truth, terminal lifecycle truth, public selectors, or delegated-runtime truth
- a minimal internal spawn-headless-close-target helper that may compose an existing spawn-headless-close-candidate result plus generic close-target derivation into bounded internal close-target metadata without becoming actual close support, child-creation truth, runtime truth, terminal lifecycle truth, public selectors, or delegated-runtime truth
- a minimal internal spawn-headless-close-target-batch helper that may compose an explicit ordered list of those same bounded internal close-target results from an existing spawn-headless-close-candidate-batch result while reusing the same single-item helper, preserving input order, keeping blocked entries in place, and failing fast on the first close-target derivation error without partial-result summaries or becoming actual close support, child-creation truth, runtime truth, terminal lifecycle truth, public selectors, or delegated-runtime truth
- a minimal internal wait-readiness layer that may derive wait preconditions and blocking reasons from runtime-context without becoming actual wait support, close support, public selectors, or lifecycle truth
- a minimal internal wait-candidate layer that may combine runtime-context and wait-readiness into a single derived object for future internal wait-oriented consumers without becoming actual wait support, close support, public selectors, or lifecycle truth
- a minimal internal wait-target layer that may derive a future internal wait-oriented target from an existing wait-candidate without becoming actual wait support, close support, public selectors, or lifecycle truth
- a minimal internal wait-request layer that may compose an existing wait-target plus explicit wait constraints such as `timeoutMs` into a future internal wait-oriented request object without becoming actual wait support, public selectors, persistence, or lifecycle truth
- a minimal internal wait-consumer preflight layer that may compose an existing wait-request plus capability-aware readiness into a future internal wait-oriented consumer object without becoming actual wait support, public selectors, persistence, or lifecycle truth
- a minimal internal wait-consume helper driven by an explicitly injected invoker that may consume an existing wait-consumer object without becoming actual wait support, polling, scheduling, public selectors, or lifecycle truth
- a minimal internal wait-consume-batch helper that may consume an explicit ordered list of existing wait-consumer objects through that same injected invoker without becoming actual wait support, polling, scheduling, public selectors, or lifecycle truth
- a minimal internal close-oriented helper chain that may derive capability-aware close-readiness, close-candidates, minimal close-targets, target-based close-requests, close-requested or close-recorded lifecycle markers, close-consumer preflight metadata, a minimal internal close-consume helper driven by an explicitly injected invoker, and a minimal internal close-consume-batch helper that consumes an explicit list of existing close-consumers through that same invoker without becoming actual close support, public selectors, or lifecycle truth

Current local docs and handoff focus:

- keep the new internal verification producer boundary inside `src/verification/*` only and above generic subprocess execution only
- keep verification payload derivation and verification execution internal-only, deterministic, non-persistent, non-manifest-backed, and compatible with the existing loose `AttemptVerification` manifest shape
- keep verification artifact summaries derived from execution results internal-only, deterministic, non-persistent, non-manifest-backed, and free of subprocess/session/runtime internals
- keep selection policy fixed to verification-summary-only ordering with loud mixed-task rejection and no execution-aware ranking inputs
- keep the new promotion/handoff candidate layer constrained to single-attempt internal consumption above artifact summaries only
- keep the new promotion/handoff aggregation layer constrained to existing promotion-candidate inputs only, with comparator-only ordering and no new artifact-aware ranking policy
- keep the new promotion/handoff explanation-ready layer constrained to existing promotion-report inputs only, with deterministic projection-only semantics and no new selection or promotion policy
- keep the new promotion/handoff decision-ready layer constrained to existing promotion-explanation inputs only, with blocker-only internal decision semantics and no new promotion or handoff policy
- keep the new promotion-target layer constrained to existing promotion-decision inputs only, with deterministic target projection semantics and no new promotion, handoff, or persistence policy
- keep the new handoff-target layer constrained to existing promotion-target inputs only, with deterministic target projection semantics and no new promotion, handoff, or persistence policy
- keep the new handoff-request layer constrained to existing handoff-target inputs only, with deterministic request projection semantics and no new promotion, handoff, or persistence policy
- keep the new handoff-consumer layer constrained to existing handoff-request inputs only, with deterministic readiness projection semantics and no new promotion, handoff execution, or persistence policy
- keep the new handoff-consume layer constrained to existing handoff-consumer inputs only, with injected-boundary invocation semantics and no new promotion, handoff scheduling, or persistence policy
- keep the new handoff-consume-batch layer constrained to existing handoff-consumer inputs only, with ordered fail-fast batch invocation semantics and no new promotion, handoff aggregation policy, or persistence policy
- keep the new handoff-apply layer constrained to existing handoff-request inputs only, with consumer-plus-consume composition semantics and no new promotion, handoff apply policy, queueing, review, merge, or persistence policy
- keep the new handoff-apply-batch layer constrained to existing handoff-request inputs only, with ordered fail-fast apply composition semantics and no new promotion, handoff aggregation policy, queueing, review, merge, or persistence policy
- keep the new handoff-target-apply layer constrained to existing handoff-target inputs only, with request-plus-apply composition semantics and no new promotion metadata, handoff aggregation policy, queueing, review, merge, or persistence policy
- keep the new handoff-target-apply-batch layer constrained to existing handoff-target inputs only, with ordered fail-fast target-plus-apply composition semantics and no new promotion metadata, handoff aggregation policy, queueing, review, merge, or persistence policy
- keep the new promotion-target-apply layer constrained to existing promotion-target inputs only, with handoff-target-plus-target-apply composition semantics and no new promotion decision/report metadata, aggregation policy, queueing, review, merge, or persistence policy
- keep the new promotion-target-apply-batch layer constrained to existing promotion-target inputs only, with ordered fail-fast promotion-target-apply composition semantics and no new promotion decision/report metadata, partial summaries, queueing, review, merge, or persistence policy
- keep future follow-up slices focused on higher internal reporting or consumer layers above the current handoff-finalization grouped reporting disposition summary helper rather than widening public verify/select/promote/merge or lifecycle surfaces

Cleanup is expected to keep manifests as audit records, target a single `attemptId`, and fail loudly on invalid manifests or unsafe path conditions.

Do not assume advanced runtime orchestration exists yet.
The current adapter layer only launches a bounded internal execution path for `codex-cli`.
It does not provide interactive runtime control, attach or resume behavior, general session lifecycle management, MCP transport execution, manifest-backed execution persistence, or public spawn/spawn-consume/spawn-effects/spawn-apply/spawn-headless-input/spawn-headless-apply/spawn-headless-execute/spawn-headless-record/spawn-headless-view/spawn-headless-context/spawn-headless-wait-candidate/spawn-headless-wait-target/spawn-headless-close-candidate/spawn-headless-close-target/wait/close semantics.
The public `doctor`, `compat probe`, and `compat smoke` commands are compatibility diagnostics surfaces only: they may report adapter implementation status, local detection state, bounded public probe results, and bounded public live-smoke results, but they must not expose internal profile/env/session/runtime-state/control-plane metadata or imply broader lifecycle support.
The public `compat probe codex-cli` surface remains intentionally narrow: it may report whether bounded local `codex exec --json` compatibility was confirmed, but it must not expose resolved executable paths, env overlays, subprocess output, canonical events, observation summaries, or any execution/session internals.
The public `compat smoke codex-cli` surface remains intentionally narrow too: it may report an env-gated bounded live smoke result for the fixed `codex exec --json` path, but it must not widen into general public execution, prompt selection, raw subprocess diagnostics, or session lifecycle support.
Treat the current smoke coverage as an optional compatibility probe only: the narrower env-gated Vitest harness currently passes in this workspace, but it still remains non-default validation rather than a public reliability guarantee and may stay green even when a real `codex exec --json` success baseline is unavailable locally.
Treat executable probing the same way: it is an internal `codex-cli` execution-helper detail used to locate a `codex` binary that really supports `exec --json`, not a generic runtime-resolution framework or a public adapter guarantee.
Treat internal `codex-cli` profile-aware execution the same way: it may best-effort pass an explicit profile name through bounded internal render or execution paths so future internal consumers can select an existing Codex profile, but it must remain internal-only, non-persistent, non-manifest-backed, non-public, and must not become provider management, a config-management surface, a public selector, or a public CLI contract.
Treat relay-compatible execution-env derivation the same way: it may best-effort derive subprocess env overlays from local Codex config, auth, or shell-export state so OpenAI-compatible relays remain usable for internal `codex exec --json` calls, but it must remain internal-only, non-persistent, non-public, and must not become a public provider switcher, a config-management surface, or a promise that direct shell sessions are repaired automatically.
That env-overlay boundary remains narrow even when profile-aware execution is present: the default runner path may still derive a best-effort home-based env overlay, but custom runners must not silently inherit that overlay unless an internal caller explicitly supplies a replacement environment resolver.
The bounded parser may preserve obvious non-JSON prelude lines, including bracket-prefixed log noise, as `unknown` events, but malformed JSON-looking records still remain loud failures.
The current manifest layer may also record thin attempt provenance through `sourceKind` and an optional `parentAttemptId`. Treat those fields as additive audit metadata only; they do not imply delegated execution, fork/resume runtime support, or session-tree lifecycle behavior.
The current internal control-plane layer may derive node references, lifecycle summaries, and parent/child indexes from provenance and observation, but those derived values are non-persistent and must not be written into `manifest.session` or exposed as public CLI contract.
The current `codex-cli` execution path may consume supplied attempt lineage and attach an internal session snapshot to execution results, but that snapshot remains adapter-internal metadata only and does not imply wait, close, attach, or resume support.
The current internal runtime-state layer, when present, must remain derived, non-persistent, and non-manifest-backed: execution-session records or indexes are future-facing internal inputs only and must not be treated as public lifecycle state or orchestration truth.
The current internal runtime-state read model, when present, must remain query-only and deterministic: selector helpers or parent/child views may serve future internal consumers, but they must not become mutable registries, wait/close controllers, or public lifecycle APIs.
The current internal runtime-context layer, when present, must remain derived, non-persistent, non-manifest-backed, and non-public: it may prepare a single execution-session context for future internal wait/close-oriented consumers, but it must not become wait/close/attach/resume support, a mutable registry or manager, or a source of lifecycle truth.
The current internal lifecycle-disposition layer, when present, must remain derived, non-persistent, non-manifest-backed, and non-public: it may centralize shared terminal/session-known/descendant-impact facts for sibling wait-readiness and close-readiness helpers, but it must not resolve adapter capability, validate parent graphs, generate blocker vocabularies, become a mutable registry or manager, write into manifests, or become a source of lifecycle truth.
The current internal spawn-readiness layer, when present, must remain derived, non-persistent, non-manifest-backed, and non-public: it may combine existing runtime-context facts, parent/child lineage visibility, and optional guardrails into delegated-child preflight blockers, but it must not become actual spawn support, public selectors, manifest-backed guardrail truth, or a mutable lifecycle manager.
The current internal spawn-budget layer, when present, must remain derived, non-persistent, non-manifest-backed, and non-public: it may project current child-count, lineage-depth, and remaining guardrail allowance from an existing execution-session context plus view for future bounded-parallelism work, but it must not become scheduler truth, queue state, runtime enforcement, manifest-backed budget truth, or a public orchestration contract.
The current internal spawn-candidate layer, when present, must remain derived, non-persistent, non-manifest-backed, and non-public: it may compose an existing execution-session context plus spawn-budget and spawn-readiness into a single-consumer internal object for future spawn-oriented consumers, but it must not become actual spawn support, public selectors, manifest-backed guardrail truth, a mutable lifecycle manager, or a source of lifecycle truth.
The current internal spawn-batch-plan layer, when present, must remain derived, non-persistent, non-manifest-backed, and non-public: it may compose an existing spawn-candidate plus a proposed sibling batch count into a bounded internal planning projection for later bounded-parallelism work, but it must not become request truth, apply truth, scheduler truth, queue state, runtime enforcement, manifest-backed budget truth, or a public orchestration contract.
The current internal spawn-batch-items layer, when present, must remain derived, non-persistent, non-manifest-backed, and non-public: it may compose an existing spawn-batch-plan plus explicit child attempt identifiers and a source kind into ordered spawn batch items for later internal batch consumers, but it must not become apply truth, headless execution truth, scheduler truth, queue state, runtime enforcement, manifest-backed batch truth, or a public orchestration contract.
The current internal spawn-batch-items-apply helper, when present, must remain derived, non-persistent, non-manifest-backed, and non-public: it may compose an existing spawn-batch-items result directly into the current spawn-apply-batch path as a bounded convenience seam, but it must not become headless execution truth, queue truth, scheduler truth, manifest-backed batch truth, or a public orchestration contract.
The current internal spawn-batch-headless-apply-items helper, when present, must remain derived, non-persistent, non-manifest-backed, and non-public: it may compose an existing spawn-batch-items result plus ordered headless execution seeds into request-preserving headless-apply batch items for later internal batch consumers, but it must not become headless execution truth, queue truth, scheduler truth, manifest-backed batch truth, or a public orchestration contract.
The current internal spawn-target layer, when present, must remain derived, non-persistent, non-manifest-backed, and non-public: it may project a minimal internal spawn-oriented target from an existing spawn-candidate, but it must not become actual spawn support, public selectors, manifest-backed guardrail truth, a mutable lifecycle manager, or a source of child-lineage decisions.
The current internal spawn-request layer, when present, must remain derived, non-persistent, non-manifest-backed, and non-public: it may compose an existing spawn-capable candidate plus an explicit child `sourceKind` choice and optional inherited guardrails into a future internal spawn-oriented request object, but it must not become actual spawn support, public selectors, manifest-backed guardrail truth, a manifest seed, a mutable registry or manager, or a source of child-attempt, branch, worktree, runtime-mode, prompt-planning, or delegated-runtime approval decisions.
The current internal spawn-lineage layer, when present, must remain derived, non-persistent, non-manifest-backed, and non-public: it may normalize a minimal child lineage object from an existing spawn-request plus an explicit child attempt identifier, but it must not become actual spawn support, a manifest seed, public selectors, parent runtime/session truth, or a source of branch, worktree, runtime-mode, prompt-planning, or delegated-runtime approval decisions.
Treat that internal spawn-oriented helper chain as lineage-aware preparation only: it may inspect resolved ancestry and inherited guardrails to shape readiness, candidate, target, request, or child-lineage helpers, but it must stop before public spawn contracts, parent-session lifecycle truth, or child branch/worktree/prompt planning.
The current internal spawn request/consume/apply path, when present, must validate and canonicalize request-shaped inputs before any injected spawn invoker runs, while keeping child-lineage derivation inside the later derived-effects step; that guardrail remains internal-only and must not be described as public spawn lifecycle support, adapter success truth, or manifest-backed runtime truth.
The current internal spawn-requested-event layer, when present, must remain derived, non-persistent, non-manifest-backed, and non-public: it may project a minimal parent-session marker from an existing spawn-request as `{ attemptId, runtime, sessionId, lifecycleEventKind: "spawn_requested" }`, but it must not reintroduce selector resolution, runtime-state reads, readiness or capability recomputation, child-attempt evidence, child-lineage truth, branch/worktree planning, manifest seeds, public selectors, or actual spawn support. Any `spawn_requested` marker in this phase remains an internal parent-session marker only; it does not imply child creation, child lineage truth, or adapter-driven spawn success.
The current internal spawn-recorded-event layer, when present, must remain derived, non-persistent, non-manifest-backed, and non-public: it may project a second minimal parent-session marker from an existing spawn-requested-event as `{ attemptId, runtime, sessionId, lifecycleEventKind: "spawn_recorded" }`, but it must not reintroduce selector resolution, runtime-state reads, readiness or capability recomputation, child-attempt evidence, child-lineage truth, branch/worktree planning, manifest seeds, public selectors, or actual spawn support. Any `spawn_recorded` marker in this phase remains an internal parent-session marker only; it does not imply child creation, child lineage truth, terminal lifecycle truth, or adapter-driven spawn success.
The current internal spawn-consume helper, when present, must remain request-based, derived, non-persistent, non-manifest-backed, and non-public: it may consume an existing spawn-request by invoking an explicitly injected spawn invoker exactly once with that existing request, but it must not reintroduce selector resolution, runtime-state reads, readiness or capability recomputation, child-lineage derivation, branch/worktree creation, prompt planning, manifest seeds, public selectors, mutable lifecycle control, terminal lifecycle truth, adapter-driven spawn success truth, or a public session-lifecycle API. Any `spawn-consume` result in this phase remains internal request-consumption metadata only; it does not imply child creation, child lineage truth, terminal lifecycle truth, branch/worktree creation, prompt planning, or adapter-driven spawn success.
The current internal spawn-consume-batch helper, when present, must remain request-based, derived, non-persistent, non-manifest-backed, and non-public: it may consume an explicit ordered list of existing spawn-requests by composing the single-request `spawn-consume` helper through that same injected invoker, but it must stay request-list-based, preserve input order, fail fast on the first injected invoker error, and must not introduce selector resolution, runtime-state reads, readiness or capability recomputation, child-lineage derivation, branch/worktree creation, prompt planning, manifest seeds, public selectors, mutable lifecycle control, terminal lifecycle truth, adapter-driven spawn success truth, per-item summary policy, or a public session-lifecycle API. Any `spawn-consume-batch` result in this phase remains internal request-consumption metadata only; it does not imply child creation, child lineage truth, terminal lifecycle truth, branch/worktree creation, prompt planning, or adapter-driven spawn success.
The current internal spawn-effects helper, when present, must remain request-based, derived, non-persistent, non-manifest-backed, and non-public: it may compose existing minimal spawn-lineage plus parent-session `spawn_requested` and `spawn_recorded` markers from an existing spawn-request and explicit child attempt identifier, but it must not invoke an adapter, reintroduce selector resolution, runtime-state reads, readiness or capability recomputation, branch/worktree creation, prompt planning, manifest seeds, public selectors, mutable lifecycle control, child creation truth, terminal lifecycle truth, or adapter-driven spawn success truth. Any `spawn-effects` result in this phase remains internal composition metadata only; it does not imply child creation, child lineage truth, terminal lifecycle truth, or actual spawn support. In particular, `effects` names future-facing composition only and must not be read as real runtime side effects.
The current internal spawn-effects-batch helper, when present, must remain request-list-based, derived, non-persistent, non-manifest-backed, and non-public: it may compose an explicit ordered list of existing spawn-effects inputs by reusing the single-request `spawn-effects` helper, but it must preserve input order, fail fast on the first derivation error, and must not introduce selector resolution, runtime-state reads, readiness or capability recomputation, branch/worktree creation, prompt planning, manifest seeds, public selectors, mutable lifecycle control, partial-result aggregation, summary policy, child creation truth, terminal lifecycle truth, or adapter-driven spawn success truth.
The current internal spawn-apply helper, when present, must remain request-based, derived, non-persistent, non-manifest-backed, and non-public: it may first consume an existing spawn-request through an explicitly injected spawn invoker and only then compose spawn-effects metadata from that same request plus an explicit child attempt identifier, but it must not reintroduce selector resolution, runtime-state reads, readiness or capability recomputation, branch/worktree creation, prompt planning, manifest seeds, public selectors, mutable lifecycle control, child creation truth, terminal lifecycle truth, or adapter-driven spawn success truth. Any `spawn-apply` result in this phase remains internal composition metadata only; it does not imply child creation, child lineage truth, terminal lifecycle truth, or actual spawn support. In particular, `apply` names future-facing composition only and must not be read as real spawn success.
The current internal spawn-apply-batch helper, when present, must remain request-list-based, derived, non-persistent, non-manifest-backed, and non-public: it may compose an explicit ordered list of existing spawn-effects inputs by reusing the single-request `spawn-apply` helper through the same injected invoker, but it must preserve input order, fail fast on the first error, and must not introduce selector resolution, runtime-state reads, readiness or capability recomputation, branch/worktree creation, prompt planning, manifest seeds, public selectors, mutable lifecycle control, partial-result aggregation, summary policy, child creation truth, terminal lifecycle truth, or adapter-driven spawn success truth.
The current internal spawn-headless-input helper, when present, must remain adapter-neutral, derived, non-persistent, non-manifest-backed, and non-public: it may compose existing spawn-effects metadata plus a minimal headless-execution seed into a future internal execution input by deriving `attempt` from `effects.lineage` and whitelisting only explicit execution fields such as `prompt`, `cwd`, `timeoutMs`, or `abortSignal`, but it must not execute a child runtime, reintroduce selector resolution, runtime-state reads, readiness or capability recomputation, branch/worktree creation, prompt planning beyond passthrough, manifest seeds, public selectors, mutable lifecycle control, child creation truth, terminal lifecycle truth, or adapter-driven spawn success truth. Any `spawn-headless-input` result in this phase remains internal input-shaping metadata only; it does not imply child creation, child runtime launch, child lineage truth beyond the explicit projection, terminal lifecycle truth, or actual delegated runtime support.
The current internal spawn-headless-input-batch helper, when present, must remain adapter-neutral, request-list-based, derived, non-persistent, non-manifest-backed, and non-public: it may compose an explicit ordered list of existing spawn-effects metadata plus execution seeds by reusing the single-request `spawn-headless-input` helper, but it must preserve input order, fail fast on the first bridge error, and must not introduce selector resolution, runtime-state reads, readiness or capability recomputation, branch/worktree creation, prompt planning beyond passthrough, manifest seeds, public selectors, mutable lifecycle control, partial-result aggregation, summary policy, child creation truth, terminal lifecycle truth, or actual delegated runtime support.
The current downstream follow-up targets above `spawn-headless-context` include bounded internal `spawn-headless-wait-candidate`, `spawn-headless-wait-target`, `spawn-headless-close-candidate`, and `spawn-headless-close-target` slices. Those targets must remain internal-only, non-public, non-persistent, non-manifest-backed, not child-creation truth, not runtime truth, not terminal lifecycle truth, and must not become actual wait or close support.
The current internal `spawn-headless-wait-request` and `spawn-headless-wait-request-batch` helpers, when present, must remain projection-only metadata above the current headless wait-target wrappers: they may derive canonical generic wait requests from available headless wait targets, preserve blocked wrappers without synthesizing fallback requests, preserve input order in batch mode, and fail fast on malformed wrappers, but they must not widen into wait-consumer/consume/apply support, polling truth, manifest-backed state, or public lifecycle semantics.
The current internal `spawn-headless-close-request` and `spawn-headless-close-request-batch` helpers, when present, must remain projection-only metadata above the current headless close-target wrappers: they may derive canonical generic close requests from available headless close targets, preserve blocked wrappers without synthesizing fallback requests, preserve input order in batch mode, and fail fast on malformed wrappers, but they must not widen into close-consumer/consume/apply support, manifest-backed state, or public lifecycle semantics.
The current internal `spawn-headless-apply` helper, when present, must remain an internal bridge helper only: it may compose existing `spawn-apply` metadata plus a whitelisted headless-execution seed into a bounded internal payload with `attempt` derived from `apply.effects.lineage`, but it must not reopen selector resolution, runtime-state reads, readiness or capability recomputation, branch/worktree creation, manifest seeds, prompt planning beyond passthrough, public selectors, or child-creation truth.
The current internal `spawn-headless-apply-batch` helper, when present, must remain request-list-based internal composition metadata only: it may preserve input order and fail fast on the first error while reusing the single-request `spawn-headless-apply` helper, but it must not widen into summary policy, public selectors, manifest-backed state, child-creation truth, lifecycle truth, or delegated runtime support.
The current internal `spawn-headless-execute` helper, when present, must remain an internal consume-path helper only: it may invoke an explicitly injected bounded headless executor with an already-shaped internal payload, but it must not widen into public CLI surfaces, manifest-backed state, lifecycle truth, delegated runtime support, or child-creation truth.
The current internal `spawn-headless-execute-batch` helper, when present, must remain request-list-based internal execution-consumption metadata only: it may preserve input order and fail fast on the first error while reusing the single-request `spawn-headless-execute` helper, but it must not widen into summary policy, public selectors, manifest-backed state, lifecycle truth, or delegated runtime support.
The current internal `spawn-headless-record` helper, when present, must remain an internal runtime-state composition helper only: it may derive a single `ExecutionSessionRecord` by composing an existing `spawn-headless-execute` result plus the generic `deriveExecutionSessionRecord(...)` helper, but it must not reopen selector resolution, runtime-state view or context derivation, readiness or capability recomputation, index building, manifest writes, public selectors, child-creation truth, terminal lifecycle truth, or delegated-runtime truth.
The current internal `spawn-headless-record-batch` helper, when present, must remain request-list-based internal runtime-state composition metadata only: it may preserve input order and fail fast on the first derivation error while reusing the single-request `spawn-headless-record` helper, but it must not widen into summary policy, public selectors, manifest-backed state, child-creation truth, lifecycle truth, or delegated runtime support.
The current internal `spawn-headless-context` helper, when present, must remain a bounded internal context-composition helper only: it may derive a single `ExecutionSessionContext` by composing an existing `spawn-headless-view` result plus the generic `deriveExecutionSessionContext(...)` helper with an explicit internal selector, but it must not reopen selector-surface expansion beyond that generic helper, runtime-state view rebuilding, readiness or capability recomputation, manifest writes, public selectors, child-creation truth, runtime truth, terminal lifecycle truth, or delegated-runtime truth.
The current internal `spawn-headless-context-batch` helper, when present, must remain batch-based internal context-composition metadata only: it may derive an explicit ordered list of `ExecutionSessionContext` results by composing an existing `spawn-headless-view-batch` result plus the generic `deriveExecutionSessionContext(...)` helper while reusing the same shared `ExecutionSessionView`, but it must preserve selector order, fail fast on the first selector or context-derivation error, expose no per-item summary contract, and it must not widen into selector-surface expansion beyond that generic helper, runtime-state view rebuilding, readiness or capability recomputation, public selectors, manifest-backed state, child-creation truth, runtime truth, terminal lifecycle truth, or delegated runtime support.
The current internal `spawn-headless-wait-candidate` helper, when present, must remain a bounded internal wait-bridge helper only: it may derive a single `ExecutionSessionWaitCandidate`-shaped object by composing an existing `spawn-headless-context` result plus the generic `deriveExecutionSessionWaitReadiness(...)` helper, but it must not call the generic selector-driven `deriveExecutionSessionWaitCandidate(...)` helper, reopen selector resolution, runtime-state views, or context derivation, widen into `wait-target` / `wait-request` / `wait-consumer` / `wait-consume`, or imply public selectors, child-creation truth, runtime truth, terminal lifecycle truth, polling truth, timeout scheduling truth, or actual wait support.
The current internal `spawn-headless-wait-candidate-batch` helper, when present, must remain batch-based internal wait-bridge metadata only: it may derive an explicit ordered list of bounded internal wait-candidate results by composing an existing `spawn-headless-context-batch` result through the single-request `spawn-headless-wait-candidate` helper, but it must preserve input order, fail fast on the first wait-readiness derivation error, expose no per-item summary contract, and it must not widen into selector resolution, runtime-state views, context derivation, generic selector-driven wait-candidate composition, `wait-target` / `wait-request` / `wait-consumer` / `wait-consume`, public selectors, manifest-backed state, child-creation truth, runtime truth, terminal lifecycle truth, polling truth, timeout scheduling truth, or actual wait support.
The current internal `spawn-headless-wait-target` helper, when present, must remain a bounded internal wait-target bridge only: it may derive a single `{ headlessWaitCandidate, target? }` object by composing an existing `spawn-headless-wait-candidate` result plus the generic `deriveExecutionSessionWaitTarget(...)` helper with the existing `headlessWaitCandidate.candidate`, but it must not reopen selector resolution, runtime-state view rebuilding, context rebuilding, wait-request/consumer/consume expansion, public selectors, manifest-backed state, actual wait support, child-creation truth, runtime truth, terminal lifecycle truth, or delegated-runtime truth.
The current internal `spawn-headless-wait-target-batch` helper, when present, must remain batch-based internal wait-target metadata only: it may derive an explicit ordered list of those same `{ headlessWaitCandidate, target? }` results by composing an existing `spawn-headless-wait-candidate-batch` result plus the single-request `spawn-headless-wait-target` helper, but it must preserve input order, keep blocked entries in place, fail fast on the first wait-target derivation error, expose no partial-result or per-item summary contract, and it must not widen into selector resolution, runtime-state view rebuilding, context rebuilding, wait-request/consumer/consume expansion, public selectors, manifest-backed state, actual wait support, child-creation truth, runtime truth, terminal lifecycle truth, or delegated runtime support.
The current internal `spawn-headless-wait-target-apply` and `spawn-headless-wait-target-apply-batch` helpers, when present, must remain apply-oriented bridge metadata only: they may compose existing headless wait-target wrappers into the generic wait target-apply path, preserve blocked wrappers without invoking, preserve input order in batch mode, and fail fast on the first supported invoker error, but they must not widen into selector resolution, runtime-state rebuilding, public wait support, polling truth, manifest-backed state, or delegated-runtime truth.
The current internal `spawn-headless-close-candidate` helper, when present, must remain a bounded internal close-oriented bridge only: it may derive a single `{ headlessContext, candidate }` object by composing an existing `spawn-headless-context` result plus the generic `deriveExecutionSessionCloseReadiness(...)` helper with the existing `headlessContext.context`, but it must not reopen selector resolution, runtime-state view rebuilding, context rebuilding, close-target/request/event/consumer/consume expansion, public selectors, manifest-backed state, actual close support, child-creation truth, runtime truth, terminal lifecycle truth, or delegated-runtime truth.
The current internal `spawn-headless-close-candidate-batch` helper, when present, must remain batch-based internal close-oriented bridge metadata only: it may derive an explicit ordered list of those same `{ headlessContext, candidate }` results by composing an existing `spawn-headless-context-batch` result plus the single-request `spawn-headless-close-candidate` helper, but it must preserve input order, fail fast on the first readiness-derivation error, expose no partial-result or per-item summary contract, and it must not widen into selector resolution, runtime-state view rebuilding, context rebuilding, close-target/request/event/consumer/consume expansion, public selectors, manifest-backed state, actual close support, child-creation truth, runtime truth, terminal lifecycle truth, or delegated runtime support.
The current internal `spawn-headless-close-target` helper, when present, must remain a bounded internal close-target bridge only: it may derive a single `{ headlessCloseCandidate, target? }` object by composing an existing `spawn-headless-close-candidate` result plus the generic `deriveExecutionSessionCloseTarget(...)` helper with the existing `headlessCloseCandidate.candidate`, but it must not reopen selector resolution, runtime-state view rebuilding, context rebuilding, close-request/event/consumer/consume expansion, public selectors, manifest-backed state, actual close support, child-creation truth, runtime truth, terminal lifecycle truth, or delegated-runtime truth.
The current internal `spawn-headless-close-target-batch` helper, when present, must remain batch-based internal close-target metadata only: it may derive an explicit ordered list of those same `{ headlessCloseCandidate, target? }` results by composing an existing `spawn-headless-close-candidate-batch` result plus the single-request `spawn-headless-close-target` helper, but it must preserve input order, keep blocked entries in place, fail fast on the first close-target derivation error, expose no partial-result or per-item summary contract, and it must not widen into selector resolution, runtime-state view rebuilding, context rebuilding, close-request/event/consumer/consume expansion, public selectors, manifest-backed state, actual close support, child-creation truth, runtime truth, terminal lifecycle truth, or delegated runtime support.
The current internal `spawn-headless-close-target-apply` and `spawn-headless-close-target-apply-batch` helpers, when present, must remain apply-oriented bridge metadata only: they may compose existing headless close-target wrappers into the generic close target-apply path, preserve blocked wrappers without invoking, preserve input order in batch mode, and fail fast on the first supported invoker error, but they must not widen into selector resolution, runtime-state rebuilding, public close support, manifest-backed state, or delegated-runtime truth.
The current internal wait-readiness layer, when present, must remain derived, non-persistent, non-manifest-backed, and non-public: it may prepare internal wait preconditions or blocking reasons for future wait-oriented consumers, but it must not become actual wait support, close support, public selectors, or a mutable lifecycle manager.
The current internal wait-candidate layer, when present, must remain derived, non-persistent, non-manifest-backed, and non-public: it may compose an existing execution-session context plus wait-readiness into a single-consumer internal object for future wait-oriented consumers, but it must not become actual wait support, close support, public selectors, a mutable lifecycle manager, or a source of lifecycle truth.
The current internal wait-target layer, when present, must remain derived, non-persistent, non-manifest-backed, and non-public: it may project a minimal future internal wait-oriented target from an existing wait-candidate, but it must not become actual wait support, close support, public selectors, a mutable lifecycle manager, or a source of lifecycle truth.
The current internal wait-request layer, when present, must remain derived, non-persistent, non-manifest-backed, and non-public: it may compose an existing wait-target plus explicit constraints such as `timeoutMs` into a minimal future internal request object, but it must not reintroduce selectors, views, or readiness decisions, and it must not become actual wait support, polling or timeout scheduling, settle policy, child policy, close coupling, public selectors, a mutable lifecycle manager, or a source of lifecycle truth.
The current internal wait-consumer preflight layer, when present, must remain derived, non-persistent, non-manifest-backed, and non-public: it may compose an existing wait-request plus capability-aware readiness into a minimal future internal consumer object, but it must not reintroduce selectors, views, contexts, candidates, targets, or readiness recomputation, and it must not become actual wait support, polling or timeout scheduling, event subscription, adapter invocation, public selectors, a mutable lifecycle manager, or a source of lifecycle truth.
The current internal wait-consume helper, when present, must remain derived, non-persistent, non-manifest-backed, and non-public: it may consume an existing wait-consumer object by either returning a structured blocked result or invoking an explicitly injected wait invoker exactly once with the existing wait-request, but it must not reintroduce selectors, runtime-state reads, readiness recomputation, capability recomputation, event projection, manifest seeds, adapter lifecycle truth, polling loops, timeout scheduling, settle policy, child policy, or actual wait success semantics. Blocked wait-consumer preflight results must not invoke the injected wait invoker, while supported wait-consumer results may invoke it exactly once with the existing wait-request only.
The current internal wait-consume-batch helper, when present, must remain derived, non-persistent, non-manifest-backed, and non-public: it may consume an explicit ordered list of existing wait-consumer objects by composing the single-consumer wait-consume helper, but it must stay consumer-list-based, preserve input order, continue past blocked entries, fail fast on the first injected invoker error from a supported entry, and must not introduce selector resolution, runtime-state reads, readiness recomputation, capability recomputation, event projection, manifest seeds, polling loops, timeout scheduling, per-item error aggregation, summary policy, or actual wait success semantics.
The current internal wait-apply helper, when present, must remain derived, non-persistent, non-manifest-backed, and non-public: it may compose an existing wait-request plus capability-aware wait-consumer derivation with the existing wait-consume helper into minimal internal apply metadata, but it must not widen into polling, scheduling, event projection, selector resolution, runtime-state truth, or public lifecycle semantics.
The current internal wait-target-apply helper, when present, must remain derived, non-persistent, non-manifest-backed, and non-public: it may compose an existing wait-target plus optional `timeoutMs` request derivation with the existing wait-apply helper into minimal internal target-apply metadata, but it must not widen into selector-driven convenience batches, public lifecycle surfaces, runtime-state truth, or actual wait success guarantees.
The current internal wait-target-apply-batch helper, when present, must remain derived, non-persistent, non-manifest-backed, and non-public: it may compose an explicit ordered list of wait targets through the existing target-to-request and single-target apply path, preserve input order, keep blocked entries in place, and fail fast on the first derivation or supported invoker error, but it must not widen into public lifecycle surfaces, runtime-state truth, or actual wait success guarantees.
The current internal close-oriented helper chain, when present, must remain derived, non-persistent, non-manifest-backed, and non-public: it may prepare capability-aware close-readiness blockers, composed close-candidates, minimal internal close-targets, target-based close-requests, close-requested and close-recorded lifecycle markers, close-consumer preflight metadata, a minimal internal close-consume helper driven by an explicitly injected invoker, or a minimal internal close-consume-batch helper driven by an explicit list of existing close-consumers plus that same invoker, but it must not reintroduce selector resolution, runtime-state reads, readiness recomputation outside the dedicated helper boundary, or actual close support, and it must not become public selectors, a mutable lifecycle manager, or a source of lifecycle truth. Any `close_requested` marker in this phase remains a future-facing internal event projection only, not close success truth; `close_recorded` is the first close-side marker that maps to shared `closed`, but it still does not imply actual close support or adapter-driven close success.
The current internal close-target-apply-batch helper, when present, must remain derived, non-persistent, non-manifest-backed, and non-public: it may compose an explicit ordered list of close targets through the existing target-to-request and single-target apply path, preserve input order, keep blocked entries in place, and fail fast on the first derivation or supported invoker error, but it must not widen into public lifecycle surfaces, runtime-state truth, or actual close success guarantees.
The current internal close-consumer preflight layer, when present, must remain derived, non-persistent, non-manifest-backed, and non-public: it may compose an existing close-request plus capability-aware readiness into a minimal future internal consumer object, but it must not reintroduce selectors, views, contexts, candidates, targets, requested-event or recorded-event inputs, and it must not become actual close support, adapter invocation, event subscription, public selectors, a mutable lifecycle manager, or a source of lifecycle truth.
The current internal close-consume helper, when present, must remain derived, non-persistent, non-manifest-backed, and non-public: it may consume an existing close-consumer object by either returning a structured blocked result or invoking an explicitly injected close invoker exactly once with the existing close-request, but it must not reintroduce selectors, runtime-state reads, readiness recomputation, capability recomputation, event projection, manifest seeds, adapter lifecycle truth, or actual close success semantics. Blocked close-consumer preflight results must not invoke the injected close invoker, while supported close-consumer results may invoke it exactly once with the existing close-request only.
The current internal close-consume-batch helper, when present, must remain derived, non-persistent, non-manifest-backed, and non-public: it may consume an explicit ordered list of existing close-consumer objects by composing the single-consumer close-consume helper, but it must stay consumer-list-based, preserve input order, continue past blocked entries, fail fast on the first injected invoker error from a supported entry, and must not introduce selector resolution, runtime-state reads, readiness recomputation, capability recomputation, event projection, manifest seeds, per-item error aggregation, summary policy, or actual close success semantics.

## Canonical Sources

When working in this repository, prefer these files in order:

1. `SPEC.md`
2. `README.md`
3. `docs/index.md`
4. `docs/specs/*`
5. `docs/compat/*`
6. `docs/research/*`

Committed docs are the canonical repository policy and contract source. Local-only `*.local.md` files may provide ephemeral operational context, but they must not override committed docs.

## Local-Only Handoff Notes

Visible local handoff files may exist at `PROJECT_STATUS.local.md` and `CODING_PHASE_PROMPT.local.md`.

- It is intentionally ignored by Git.
- It may contain current progress, next steps, and local research pointers.
- Treat it as operational context, not public policy or canonical repository truth.

## Repository Rules

- Keep public documents sanitized and reusable.
- Do not place secrets, machine-specific paths, transcripts, or raw scratch notes in committed docs.
- Prefer updating existing canonical docs over creating overlapping documents.
- Treat compatibility claims carefully: document supported, experimental, and future targets separately.

## Compatibility Intent

Tier 1:

- Claude Code
- Codex CLI
- Gemini CLI
- OpenCode

Experimental:

- OpenClaw
- Other coding-agent CLIs through a generic adapter contract

## Implementation Boundary

The current implementation boundary should stay narrow:

- preserve the existing `Phase 1: Core Scaffold` and `Phase 2: Worktree Lifecycle` foundations while prioritizing Phase 4 baseline accuracy and thin internal Phase 5 continuation
- keep machine-readable output and runtime manifests clearer than human-readable text
- avoid widening public adapter, execution, verification-ranking, report, or lifecycle surfaces unless a spec or RFC explicitly justifies that promotion
- keep internal architecture flexible unless a spec explicitly defines a public contract
- keep adapter work thin and contract-first even with the existing bounded internal `codex-cli` execution contract

## Future Work Tracking

Use public RFCs and ADRs for durable decisions.
Use the local handoff file for session continuity and short-lived operational notes.
This repository now has a usable Git commit baseline, so implementation windows should actively use non-destructive Git commits, branches, and worktrees as archival checkpoints for each completed thin slice or debugging milestone.
